/**
 * Defines the column data types which then identifies how the values should be displayed in the table.
 */
export enum CrudTableColumnType
{
    DATE,
    STRING,
    CURRENCY,
    NUMBER,
    PERCENT,
    GAIN_LOSS_PERCENT,
    COMMENTS,
    NOTES_SOURCE,
    CUSTOM,
    MARKET_CAP
}
