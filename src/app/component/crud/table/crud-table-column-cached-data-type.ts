/**
 * This enum defines the types of objects that are cached and thus their values to display in the table need to come
 * from the appropriate cache.
 */
export enum CrudTableColumnCachedDataType
{
    STOCK_QUOTE,
    STOCK_PRICE_QUOTE,
    STOCK_COMPANY,
    STOCK_GAINS_LOSSES,
    STOCK_ANALYST_CONSENSUS
}
